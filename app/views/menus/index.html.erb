<p id="notice"><%= notice %></p>

<h1>Menu di Ngant.in</h1>

<table>
  <thead>
    <tr>
      <th colspan="1">Gambar</th>
      <th>Nama</th>
      <th>Harga</th>
      <th>Rating</th>
      <th>Warung</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @menus.each do |menu| %>
      <tr>
        <td><%= image_tag menu.image.variant(resize_to_fit: [400, 600]) %></td>
        <td><%= menu.nama %></td>
        <td><%= menu.harga %></td>
        <td><%= menu.rating %></td>
        <td><%= menu.warung %></td>
        <td><%= link_to 'Show', menu %></td>
        <td><%= link_to 'Edit', edit_menu_path(menu) %></td>
        <td><%= link_to 'Destroy', menu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Menu', new_menu_path %>
